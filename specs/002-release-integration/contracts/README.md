# Contracts

This feature does not expose any API contracts.

Bumpkin is a CLI tool - all interfaces are:
- Command-line flags and arguments
- Configuration file (`.bumpkin.yml`)
- Environment variables for hooks

See [data-model.md](../data-model.md) for the configuration schema.
